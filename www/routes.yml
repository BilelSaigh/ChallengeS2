/:
  controller: Main
  action: index

/dashboard:
  controller: Main
  action: dashboard

/contact:
  controller: Main
  action: contact

/login:
  controller: Security
  action: login

/logout:
  controller: Security
  action: logout

/register:
  controller: Security
  action: register

/confirmation:
  controller: Security
  action: confirmation

/users:
  controller : User
  action : showUsers

/deleteuser:
  controller: User
  action: deleteUsers

/edituser:
  controller: User
  action: setting

/pagebuilder:
  controller: Page
  action: newpage

/profil:
  controller: User
  action: home

/signout:
  controller: Security
  action: logout

/error404:
  controller: Error
  action: error404